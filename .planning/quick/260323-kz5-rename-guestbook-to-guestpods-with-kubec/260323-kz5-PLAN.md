---
phase: quick
plan: 260323-kz5
type: execute
wave: 1
depends_on: []
files_modified:
  - web/index.html
  - web/src/components/SubmissionForm.tsx
  - web/src/style.css
  - web/src/admin/PodList.tsx
  - web/src/app.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Browser tab shows 'Guestpods' as the page title"
    - "Submission form displays 'Guestpods' as the app name and 'KubeCon 2026' as a subtitle"
    - "All user-facing text references say 'Guestpods' instead of 'guestbook'"
  artifacts:
    - path: "web/index.html"
      provides: "Updated HTML title and meta description"
      contains: "Guestpods"
    - path: "web/src/components/SubmissionForm.tsx"
      provides: "App title and subtitle in the form card"
      contains: "Guestpods"
  key_links: []
---

<objective>
Rename the user-facing brand from "KubeCon Guestbook" to "Guestpods" and add a "KubeCon 2026" subtitle on the entry form.

Purpose: Rebrand the app with a more memorable, pod-themed name and tie it to the KubeCon 2026 event.
Output: Updated HTML title, meta description, form heading with subtitle, and all user-facing "guestbook" references replaced with "Guestpods".
</objective>

<execution_context>
@/Users/niklas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/niklas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@web/index.html
@web/src/components/SubmissionForm.tsx
@web/src/style.css
@web/src/admin/PodList.tsx
@web/src/app.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HTML title, meta, and user-facing guestbook references</name>
  <files>web/index.html, web/src/admin/PodList.tsx, web/src/app.tsx, web/src/components/SubmissionForm.tsx</files>
  <action>
1. In `web/index.html`:
   - Change `<title>KubeCon Guestbook</title>` to `<title>Guestpods</title>`
   - Update the meta description to: `"Join the cluster! Sign the Guestpods and appear as a pod in a live Kubernetes visualization."`

2. In `web/src/admin/PodList.tsx` line 18:
   - Change "submit the guestbook form" to "sign the Guestpods"

3. Do NOT change the localStorage key `guestbook_submission` in `app.tsx` or `SubmissionForm.tsx` -- changing it would reset submission state for anyone who already submitted, causing them to see the form again instead of the visualization. This is an internal storage key, not user-facing text.

4. Do NOT change the Go module path, Helm chart template names, binary name, or any infrastructure identifiers. Those are internal names, not user-facing branding.
  </action>
  <verify>
    <automated>cd /Users/niklas/Coding/kubecon-guestbook && grep -q "Guestpods" web/index.html && grep -q "Guestpods" web/src/admin/PodList.tsx && ! grep -q "KubeCon Guestbook" web/index.html && echo "PASS: User-facing text updated"</automated>
  </verify>
  <done>HTML title says "Guestpods", meta description updated, admin empty state says "sign the Guestpods", no user-facing "guestbook" text remains in the web UI.</done>
</task>

<task type="auto">
  <name>Task 2: Add app title and KubeCon 2026 subtitle to submission form</name>
  <files>web/src/components/SubmissionForm.tsx, web/src/style.css</files>
  <action>
1. In `web/src/components/SubmissionForm.tsx`, inside the `.card` div and BEFORE the `{bannerError && ...}` block (around line 94-95), add a header block:

```tsx
<div class="form-brand">
  <h1 class="form-title">Guestpods</h1>
  <p class="form-subtitle">KubeCon 2026</p>
</div>
```

2. In `web/src/style.css`, add styles for the brand header. Place them after the `.card` block (after line 56). The styles should:

```css
/* Form brand header */
.form-brand {
  text-align: center;
  margin-bottom: var(--space-lg);
}

.form-title {
  font-size: 28px;
  font-weight: 700;
  color: var(--color-text-primary);
  letter-spacing: -0.5px;
  margin-bottom: var(--space-xs);
}

.form-subtitle {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-text-secondary);
  text-transform: uppercase;
  letter-spacing: 2px;
}
```

The design: "Guestpods" as a large bold heading, "KubeCon 2026" as a smaller uppercase subtitle below it, both centered. Uses existing CSS variables for colors and spacing. Matches the dark theme aesthetic.
  </action>
  <verify>
    <automated>cd /Users/niklas/Coding/kubecon-guestbook && grep -q "form-brand" web/src/components/SubmissionForm.tsx && grep -q "Guestpods" web/src/components/SubmissionForm.tsx && grep -q "KubeCon 2026" web/src/components/SubmissionForm.tsx && grep -q "form-brand" web/src/style.css && grep -q "form-title" web/src/style.css && grep -q "form-subtitle" web/src/style.css && echo "PASS: Title and subtitle added with styles"</automated>
  </verify>
  <done>Submission form displays "Guestpods" as a centered h1 heading with "KubeCon 2026" as an uppercase subtitle below it, styled consistently with the dark theme.</done>
</task>

</tasks>

<verification>
- `cd web && npx vite build` completes without errors (TypeScript compilation passes)
- Browser tab shows "Guestpods" as the title
- No user-facing text contains "guestbook" (case-insensitive grep of rendered components should find zero matches outside of localStorage keys and comments)
</verification>

<success_criteria>
- HTML title reads "Guestpods"
- Meta description references "Guestpods"
- Submission form shows "Guestpods" heading and "KubeCon 2026" subtitle
- Admin empty state says "sign the Guestpods"
- No user-facing "guestbook" text remains in the web UI
- Frontend builds without errors
</success_criteria>

<output>
After completion, create `.planning/quick/260323-kz5-rename-guestbook-to-guestpods-with-kubec/260323-kz5-SUMMARY.md`
</output>
