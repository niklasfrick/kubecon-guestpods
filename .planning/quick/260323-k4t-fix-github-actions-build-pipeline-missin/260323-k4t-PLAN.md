---
phase: quick
plan: 260323-k4t
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release.yaml
autonomous: true
requirements: []
must_haves:
  truths:
    - "GitHub Actions release workflow produces a valid Docker image tag when triggered by a v* git tag"
    - "Build-and-push step always receives a non-empty tags value"
  artifacts:
    - path: ".github/workflows/release.yaml"
      provides: "Release workflow with robust tag metadata"
      contains: "type=sha"
  key_links:
    - from: "docker/metadata-action step"
      to: "docker/build-push-action tags input"
      via: "steps.meta.outputs.tags"
      pattern: "tags:.*steps\\.meta\\.outputs\\.tags"
---

<objective>
Fix the GitHub Actions release workflow so the build-and-push step always has a valid image tag when pushing to GHCR.

Purpose: The CI pipeline fails with "ERROR: failed to build: tag is needed when pushing to registry" because the `docker/metadata-action` outputs can be empty under certain tag formats, leaving `tags` empty in the build-and-push step.

Output: A working `.github/workflows/release.yaml` that reliably tags Docker images.
</objective>

<execution_context>
@/Users/niklas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/niklas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/release.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix metadata-action tag patterns and add fallback tag</name>
  <files>.github/workflows/release.yaml</files>
  <action>
Edit `.github/workflows/release.yaml` to fix the metadata-action tag configuration:

1. In the "Extract metadata" step, update the `tags` block to include a SHA-based fallback tag that always produces output, and add `type=raw` with the git ref name for robustness:

```yaml
tags: |
  type=semver,pattern={{version}}
  type=semver,pattern={{major}}.{{minor}}
  type=sha,prefix=,format=short
```

The `type=sha` line ensures that even if the semver patterns fail to match (e.g., tag format mismatch), there is always at least one valid tag produced.

2. Verify the `tags` input on the build-and-push step still references `${{ steps.meta.outputs.tags }}` (it already does -- do not change this).

3. Verify the `labels` input still references `${{ steps.meta.outputs.labels }}` (it already does -- do not change this).

The root cause: `type=semver` patterns only produce output when the git tag strictly matches semver format. If the tag doesn't parse as valid semver, the metadata action outputs an empty string, and buildx fails because `push: true` requires at least one tag.
  </action>
  <verify>
    <automated>grep -q "type=sha" .github/workflows/release.yaml && grep -q "type=semver,pattern={{version}}" .github/workflows/release.yaml && grep -q 'tags:.*steps\.meta\.outputs\.tags' .github/workflows/release.yaml && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>The metadata-action step produces at least one tag (SHA-based) regardless of git tag format, and the semver patterns remain for proper version tagging. The build-and-push step still consumes tags from the metadata step output.</done>
</task>

</tasks>

<verification>
- `grep "type=sha" .github/workflows/release.yaml` shows the fallback tag pattern
- `grep "type=semver" .github/workflows/release.yaml` shows both semver patterns preserved
- The workflow YAML is valid (no syntax errors)
- The build-and-push step still references `steps.meta.outputs.tags`
</verification>

<success_criteria>
- release.yaml has a SHA-based fallback tag in the metadata-action step
- Both semver tag patterns are preserved for proper version tagging
- The build-and-push step always receives at least one non-empty tag
</success_criteria>

<output>
After completion, create `.planning/quick/260323-k4t-fix-github-actions-build-pipeline-missin/260323-k4t-SUMMARY.md`
</output>
