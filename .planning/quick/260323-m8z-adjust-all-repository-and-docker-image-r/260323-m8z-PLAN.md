---
phase: quick
plan: 260323-m8z
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - main.go
  - chart/Chart.yaml
  - chart/values.yaml
  - chart/templates/_helpers.tpl
  - chart/templates/deployment.yaml
  - chart/templates/service.yaml
  - chart/templates/pvc.yaml
  - chart/templates/configmap.yaml
  - chart/templates/httproute.yaml
  - chart/templates/NOTES.txt
  - web/package.json
  - web/package-lock.json
autonomous: true
requirements: []

must_haves:
  truths:
    - "All source code references point to niklasfrick/kubecon-guestpods"
    - "Go module compiles with new module path"
    - "Helm chart renders with guestpods naming"
    - "Docker image repository in values.yaml uses kubecon-guestpods"
  artifacts:
    - path: "go.mod"
      contains: "module github.com/niklasfrick/kubecon-guestpods"
    - path: "chart/Chart.yaml"
      contains: "name: kubecon-guestpods"
    - path: "chart/values.yaml"
      contains: "ghcr.io/niklasfrick/kubecon-guestpods"
  key_links:
    - from: "main.go"
      to: "go.mod"
      via: "import path must match module declaration"
      pattern: "github.com/niklasfrick/kubecon-guestpods/server"
    - from: "chart/templates/*.yaml"
      to: "chart/templates/_helpers.tpl"
      via: "template include calls must match define names"
      pattern: "guestpods\\."
---

<objective>
Rename all references from the old repository (kubecon-guestbook / github.com/niklas/kubecon-guestbook) to the new repository (kubecon-guestpods / github.com/niklasfrick/kubecon-guestpods).

Purpose: The project has been renamed and moved to https://github.com/niklasfrick/kubecon-guestpods. All code, module paths, Docker image references, and Helm chart naming must reflect the new location.
Output: All source files consistently reference the new repository name and owner.
</objective>

<execution_context>
@/Users/niklas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/niklas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Go module path and imports</name>
  <files>go.mod, main.go</files>
  <action>
    1. In `go.mod` line 1, change the module path from `github.com/niklas/kubecon-guestbook` to `github.com/niklasfrick/kubecon-guestpods`.

    2. In `main.go` line 15, change the import from `"github.com/niklas/kubecon-guestbook/server"` to `"github.com/niklasfrick/kubecon-guestpods/server"`.

    No other `.go` files import the module path (server package files use relative package references). The `embed.go` file has no module path references.

    After editing, run `go build ./...` to verify the module compiles cleanly.
  </action>
  <verify>
    <automated>cd /Users/niklas/Coding/kubecon-guestbook && go build ./... 2>&1 && echo "PASS: Go build successful" || echo "FAIL: Go build failed"</automated>
  </verify>
  <done>go.mod declares module github.com/niklasfrick/kubecon-guestpods, main.go imports github.com/niklasfrick/kubecon-guestpods/server, `go build ./...` exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Update Helm chart naming and Docker image reference</name>
  <files>chart/Chart.yaml, chart/values.yaml, chart/templates/_helpers.tpl, chart/templates/deployment.yaml, chart/templates/service.yaml, chart/templates/pvc.yaml, chart/templates/configmap.yaml, chart/templates/httproute.yaml, chart/templates/NOTES.txt</files>
  <action>
    1. **chart/Chart.yaml**: Change `name: kubecon-guestbook` to `name: kubecon-guestpods`. Update description from "guestbook" to "guestpods" if present.

    2. **chart/values.yaml**: Change `repository: ghcr.io/niklasfrick/kubecon-guestbook` to `repository: ghcr.io/niklasfrick/kubecon-guestpods`.

    3. **chart/templates/_helpers.tpl**: Rename ALL template definitions and references from `guestbook.*` to `guestpods.*`:
       - `guestbook.name` -> `guestpods.name` (line 4)
       - `guestbook.fullname` -> `guestpods.fullname` (line 11)
       - `guestbook.labels` -> `guestpods.labels` (line 27)
       - `guestbook.selectorLabels` -> `guestpods.selectorLabels` (line 37)
       - `guestbook.image` -> `guestpods.image` (line 45)
       - All `include "guestbook.*"` calls within _helpers.tpl itself (lines 28, 29, 38)

    4. **chart/templates/deployment.yaml**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 4, 6, 13, 17, 21). Keep container name `guestbook` on line 20 -- actually change it to `guestpods` for consistency.

    5. **chart/templates/service.yaml**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 4, 6, 15).

    6. **chart/templates/pvc.yaml**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 5, 7).

    7. **chart/templates/configmap.yaml**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 4, 6).

    8. **chart/templates/httproute.yaml**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 5, 7, 24).

    9. **chart/templates/NOTES.txt**: Change all `include "guestbook.*"` to `include "guestpods.*"` (lines 6, 9, 12).

    The simplest approach: in _helpers.tpl and all template files, do a global find-replace of `guestbook.` with `guestpods.` for template names, and in deployment.yaml change the container name from `guestbook` to `guestpods`.

    After editing, run `helm lint chart/` and `helm template test chart/` to verify.
  </action>
  <verify>
    <automated>cd /Users/niklas/Coding/kubecon-guestbook && helm lint chart/ 2>&1 && helm template test chart/ 2>&1 | head -5 && echo "PASS: Helm chart lints and renders" || echo "FAIL"</automated>
  </verify>
  <done>Chart.yaml name is kubecon-guestpods. values.yaml image repository is ghcr.io/niklasfrick/kubecon-guestpods. All template helper names use guestpods prefix. helm lint and helm template succeed without errors.</done>
</task>

<task type="auto">
  <name>Task 3: Update web package name</name>
  <files>web/package.json, web/package-lock.json</files>
  <action>
    1. In `web/package.json` line 2, change `"name": "kubecon-guestbook-web"` to `"name": "kubecon-guestpods-web"`.

    2. In `web/package-lock.json`, change the name field on line 2 from `"kubecon-guestbook-web"` to `"kubecon-guestpods-web"`, and also on line 7 where the nested package name appears.

    Do NOT run `npm install` as that would be unnecessary -- this is just a name field change. But do verify the JSON is valid.
  </action>
  <verify>
    <automated>cd /Users/niklas/Coding/kubecon-guestbook/web && node -e "const p = require('./package.json'); if(p.name !== 'kubecon-guestpods-web') { throw new Error('name not updated'); } console.log('PASS: package.json name is ' + p.name);" && node -e "const p = require('./package-lock.json'); if(p.name !== 'kubecon-guestpods-web') { throw new Error('name not updated'); } console.log('PASS: package-lock.json name is ' + p.name);"</automated>
  </verify>
  <done>web/package.json and web/package-lock.json both have name "kubecon-guestpods-web". JSON is valid.</done>
</task>

</tasks>

<verification>
All references to the old repository name have been updated:
- `grep -r "kubecon-guestbook" --include="*.go" --include="*.yaml" --include="*.yml" --include="*.json" --include="*.tpl" --include="*.txt" . | grep -v .planning/ | grep -v node_modules/` returns NO results
- `go build ./...` succeeds
- `helm lint chart/` succeeds
- `helm template test chart/` renders successfully
</verification>

<success_criteria>
- Zero occurrences of "kubecon-guestbook" in non-planning source files (Go, YAML, JSON, TPL, TXT)
- Zero occurrences of "github.com/niklas/kubecon-guestbook" anywhere in source
- Go module compiles: `go build ./...` exits 0
- Go tests pass: `go test ./...` exits 0
- Helm chart is valid: `helm lint chart/` exits 0
- Docker image reference points to ghcr.io/niklasfrick/kubecon-guestpods
</success_criteria>

<output>
After completion, create `.planning/quick/260323-m8z-adjust-all-repository-and-docker-image-r/260323-m8z-SUMMARY.md`
</output>
