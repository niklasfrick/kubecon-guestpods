---
phase: quick
plan: 260323-ly6
type: execute
wave: 1
depends_on: []
files_modified:
  - chart/values.yaml
  - chart/templates/certificate.yaml
  - chart/templates/httproute.yaml
  - chart/templates/extraobjects.yaml
autonomous: true
requirements: [quick-260323-ly6]

must_haves:
  truths:
    - "TLS/cert-manager configuration is completely removed from the chart"
    - "HTTPRoute supports optional annotations via values"
    - "extraObjects allows deploying arbitrary Kubernetes manifests alongside the chart"
  artifacts:
    - path: "chart/values.yaml"
      provides: "Updated values without tls section, with httpRoute.annotations and extraObjects"
    - path: "chart/templates/httproute.yaml"
      provides: "HTTPRoute template rendering optional annotations"
    - path: "chart/templates/extraobjects.yaml"
      provides: "Template iterating over extraObjects list and rendering each as a manifest"
  key_links:
    - from: "chart/values.yaml"
      to: "chart/templates/httproute.yaml"
      via: "httpRoute.annotations consumed in template"
      pattern: "Values\\.httpRoute\\.annotations"
    - from: "chart/values.yaml"
      to: "chart/templates/extraobjects.yaml"
      via: "extraObjects list iterated in template"
      pattern: "Values\\.extraObjects"
---

<objective>
Update the Helm chart with three changes: (1) remove the cert-manager TLS Certificate resource and its values, (2) add an annotations option to HTTPRoute so users can set arbitrary annotations (e.g., for external-dns or cert-manager Gateway API integration), and (3) add an extraObjects value that renders arbitrary Kubernetes manifests alongside the chart.

Purpose: Makes the chart more flexible for different cluster setups -- TLS can be handled by the Gateway/infrastructure layer rather than a dedicated Certificate resource, annotations allow integration with external-dns or cert-manager gateway-shim, and extraObjects enables deploying companion resources (Secrets, ConfigMaps, etc.) without separate manifests.

Output: Updated chart with TLS removed, HTTPRoute annotations support, and extraObjects capability.
</objective>

<execution_context>
@/Users/niklas/.claude/get-shit-done/workflows/execute-plan.md
@/Users/niklas/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@chart/values.yaml
@chart/templates/certificate.yaml
@chart/templates/httproute.yaml
@chart/templates/_helpers.tpl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove TLS/cert-manager config and delete Certificate template</name>
  <files>chart/values.yaml, chart/templates/certificate.yaml</files>
  <action>
1. Delete the file `chart/templates/certificate.yaml` entirely -- the cert-manager Certificate resource is no longer managed by this chart.

2. In `chart/values.yaml`, remove the entire `tls:` section (lines 45-50 which contain enabled, issuer, issuerKind, secretName). Leave all other sections intact.
  </action>
  <verify>
    <automated>helm template test /Users/niklas/Coding/kubecon-guestbook/chart 2>&1 | grep -c "Certificate" | grep -q "0" && echo "PASS: no Certificate resource" || echo "FAIL"</automated>
  </verify>
  <done>certificate.yaml deleted, tls section removed from values.yaml, helm template renders without Certificate resource and without errors</done>
</task>

<task type="auto">
  <name>Task 2: Add annotations support to HTTPRoute and create extraObjects template</name>
  <files>chart/templates/httproute.yaml, chart/templates/extraobjects.yaml, chart/values.yaml</files>
  <action>
1. In `chart/values.yaml`, add an `annotations` map under `httpRoute:` (default empty `{}`). Place it right after `enabled: true` so it reads:
```yaml
httpRoute:
  enabled: true
  annotations: {}
  gateway:
    name: main-gateway
    namespace: default
  domain: guestbook.example.com
```

2. In `chart/templates/httproute.yaml`, add annotation rendering in metadata. After the labels block, add:
```yaml
  {{- with .Values.httpRoute.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
```

3. In `chart/values.yaml`, add an `extraObjects` list at the bottom of the file (after `probes:`):
```yaml
# Extra Kubernetes objects to deploy alongside the chart
# Each entry is a complete Kubernetes manifest
extraObjects: []
# Example:
# extraObjects:
#   - apiVersion: v1
#     kind: Secret
#     metadata:
#       name: my-secret
#     stringData:
#       key: value
```

4. Create `chart/templates/extraobjects.yaml` with:
```yaml
{{- range .Values.extraObjects }}
---
{{ toYaml . }}
{{- end }}
```
  </action>
  <verify>
    <automated>helm template test /Users/niklas/Coding/kubecon-guestbook/chart --set "httpRoute.annotations.external-dns\\.alpha\\.kubernetes\\.io/hostname=test.example.com" 2>&1 | grep -q "external-dns" && echo "PASS: annotations rendered" || echo "FAIL: annotations not found"</automated>
  </verify>
  <done>HTTPRoute template renders annotations when provided and omits them when empty. extraObjects template exists and renders arbitrary manifests from values. helm template succeeds with no errors for default values.</done>
</task>

</tasks>

<verification>
Run full helm template with default values -- should succeed with no errors and produce no Certificate resource:
```bash
helm template test chart/ 2>&1
```

Run helm template with custom annotations and extraObjects:
```bash
helm template test chart/ \
  --set 'httpRoute.annotations.external-dns\.alpha\.kubernetes\.io/hostname=guestbook.example.com' \
  --set-json 'extraObjects=[{"apiVersion":"v1","kind":"ConfigMap","metadata":{"name":"test-extra"},"data":{"key":"value"}}]'
```
Verify: annotations appear on HTTPRoute, ConfigMap appears in output, no Certificate resource.
</verification>

<success_criteria>
- `helm template` with default values produces valid output with no Certificate resource and no TLS references
- HTTPRoute includes annotations when httpRoute.annotations is set, omits annotations block when empty
- extraObjects renders arbitrary manifests when provided, produces nothing when empty list
- No helm template errors or warnings
</success_criteria>

<output>
After completion, create `.planning/quick/260323-ly6-update-helm-chart-remove-cert-tls-config/260323-ly6-SUMMARY.md`
</output>
