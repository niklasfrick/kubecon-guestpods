apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "guestpods.fullname" . }}
  labels:
    {{- include "guestpods.labels" . | nindent 4 }}
data:
  BASE_URL: {{ .Values.config.baseURL | quote }}
  ADDR: {{ .Values.config.addr | quote }}
